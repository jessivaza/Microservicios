# Configuracion del servidor
server.port=3001 
spring.application.name=user-service

# Configuracion de la Base de Datos MySQL (User Service DB)
spring.datasource.url=jdbc:mysql://localhost:3306/user_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=User123.
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuracion de JPA (Hibernate)
spring.jpa.hibernate.ddl-auto=update 
spring.jpa.show-sql=true

# Excluir auto-configuración de seguridad por defecto
# Nota: La auto-exclusión se hace mejor en UserServiceApplication.java, pero se deja aquí por referencia.